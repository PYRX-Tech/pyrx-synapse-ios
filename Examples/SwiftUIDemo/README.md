# SwiftUIDemo (placeholder)

The full SwiftUI sample app lands in **Phase 8.4a PR 6** (Task 8.4a.13).

This directory is a placeholder so the workspace layout is stable from PR 1
and the `.swiftlint.yml` excludes path (`Examples/SwiftUIDemo/Pods`) is valid.

When the sample arrives, it will demonstrate:

- `Pyrx.shared.initialize(config:)` on app launch
- `identify`, `track`, `reset` from a settings screen
- Push registration + foreground notification handling
- Attribution capture from `UIApplication.openURL`
- Privacy toggles (opt-out, IDFA)

See [`DEVELOPMENT_PLAN.md`](https://github.com/PYRX-Tech/pyrx.synapse/blob/master/DEVELOPMENT_PLAN.md)
§8.4a tasks 8.4a.12 + 8.4a.13.
